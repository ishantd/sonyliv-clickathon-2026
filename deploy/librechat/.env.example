# Template for /etc/sonyliv/librechat.env on the box.
#
#   sudo install -m 600 -o root -g root /dev/null /etc/sonyliv/librechat.env
#   sudo vi /etc/sonyliv/librechat.env      # paste and fill this in
#
# Root-owned 0600, same convention as sonyliv.env and mcp.env: deploy-librechat.sh never
# writes it, so no secret travels through the deploy path. Docker reads it as root when it
# starts the containers.
#
# Generate the four crypto values with:
#   CREDS_KEY:           openssl rand -hex 32
#   CREDS_IV:            openssl rand -hex 16
#   JWT_SECRET:          openssl rand -hex 32
#   JWT_REFRESH_SECRET:  openssl rand -hex 32
#   LITELLM_MASTER_KEY:  echo "sk-$(openssl rand -hex 24)"
# LibreChat refuses to start on the defaults it ships, which is the correct behaviour --
# those values are public.

# --- the model, via LiteLLM -------------------------------------------------
GEMINI_API_KEY=

# LiteLLM's own auth. LibreChat presents this as its apiKey; nothing else may call the
# proxy. Must start with sk-.
LITELLM_MASTER_KEY=

# --- Langfuse Cloud ---------------------------------------------------------
# Read by BOTH containers, and they trace different halves:
#
#   LibreChat  emits an `AgentRun` trace per user turn -- the LangGraph structure, and a
#              TOOL span per MCP call carrying its arguments and its output. This is
#              native in v0.8.7; it needs no config beyond these three variables.
#   LiteLLM    emits a generation per model call with token counts and cost, which the
#              LibreChat spans do not carry.
#
# Two traces per turn, deliberately: neither is a superset of the other. To keep only
# LibreChat's, drop success_callback/failure_callback from litellm-config.yaml.
#
# LANGFUSE_HOST is what the SDKs call LANGFUSE_BASE_URL in the repo .env -- same value,
# different variable name, and getting it wrong silently sends traces to
# cloud.langfuse.com instead of erroring.
LANGFUSE_HOST=https://cloud.langfuse.com
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=

# --- the serving layer ------------------------------------------------------
# Same value as SONYLIV_MCP_TOKEN in /etc/sonyliv/mcp.env. LibreChat sends it as the bearer
# on every MCP call; if the two drift, every tool call returns 401 and the model reports
# that it has no data rather than that it is unauthorised.
SONYLIV_MCP_TOKEN=

# --- LibreChat crypto -------------------------------------------------------
CREDS_KEY=
CREDS_IV=
JWT_SECRET=
JWT_REFRESH_SECRET=

# --- LibreChat behaviour ----------------------------------------------------
# Both must be the public https origin, or OAuth-less login still works but absolute links
# in the UI point at localhost. On the demo box LibreChat has its own hostname, so this is
# https://chat.fastandfurious.live and NOT the apex -- the apex is the fleet dashboard.
DOMAIN_CLIENT=https://your-host
DOMAIN_SERVER=https://your-host

# READ THIS BEFORE COPYING. The note that used to be here said "this box has no public
# IPv4 and is VPC-only, so it is left open". That is no longer true and has not been since
# the box got an Elastic IP and three public DNS names: chat.fastandfurious.live answers to
# anyone on the internet on :443.
#
# So `true` here means anyone who finds the hostname can create an account and spend the
# Gemini key below. LibreChat has no anonymous or guest mode -- the official docs offer
# only which login methods are allowed, never none -- so open registration is as close to
# "no login" as this app gets, and that is exactly why it is not a safe default.
#
# For a judged demo the tighter option is to leave this false and hand out the one seeded
# demo account instead (LIBRECHAT_EMAIL/LIBRECHAT_PASSWORD below); users can also be
# created directly with LibreChat's create-user script while registration is closed. If you
# do open it, close it again when the judging window ends.
ALLOW_REGISTRATION=true
ALLOW_EMAIL_LOGIN=true
ALLOW_SOCIAL_LOGIN=false
ALLOW_UNVERIFIED_EMAIL_LOGIN=true

# Off: the box has no mail transport, and with it on, registration silently fails at the
# verification step.
EMAIL_HOST=

# --- the agent's owner ---------------------------------------------------------
# sync-agent.sh authenticates as this account to create/update the Agent that owns the
# MCP tools. It must be a registered LibreChat user; create it once through the UI or
# POST /api/auth/register.
#
# The Agent is not a nicety. Without it, the MCP server has to be attached from the
# chat-area dropdown once per conversation, and when it is not, the analyst prompt --
# which says to answer only from a tool call -- leaves the model with no legal move.
# The observed result was an empty assistant bubble with no error anywhere.
LIBRECHAT_EMAIL=
LIBRECHAT_PASSWORD=

# Who else may use the agent. Comma-separated emails of ALREADY-REGISTERED accounts.
#
# LibreChat 0.8 puts agents behind per-resource ACLs: creating one grants access to its
# author and to nobody else. So without this, the analyst works perfectly for
# LIBRECHAT_EMAIL and every other account -- including your teammates' -- gets
#
#     {"error":"Forbidden","message":"Insufficient permissions to access this agent"}
#
# as a red bubble under their own first message, with nothing in the logs and nothing
# actually wrong with the model, the MCP server or the prompt. It reads as a broken
# deployment and it is a missing grant. sync-agent.sh issues the grants on every run.
#
# Judges do NOT need to be listed: they are handed LIBRECHAT_EMAIL, which owns the agent.
# This is for the team. Deliberately not `public: true` -- with ALLOW_REGISTRATION=true
# above, public means anyone who signs up can drive the agent and spend the Gemini key.
#
# An address with no account is skipped with a warning rather than failing the deploy;
# the person has to sign up first. Unset means owner-only.
LIBRECHAT_SHARE_EMAILS=

# --- code interpreter (optional) -----------------------------------------------
# Leave EMPTY unless you have a key. Charts already work without it: the agent has
# `artifacts` enabled, so a charting question returns a Recharts component that LibreChat
# renders as an interactive chart. Code interpreter buys executed Python (matplotlib PNGs,
# file in/out), not charts per se.
#
# It cannot be self-hosted on this box. The open-source service runs sandboxes under KVM,
# and this instance has no /dev/kvm and no vmx/svm flags — an ordinary EC2 guest, not a
# metal one. A .metal instance, or LibreChat's hosted key, are the two options.
#
# sync-agent.sh adds the execute_code tool only when this is set, because a tool that is
# present but unconfigured fails on every call and reads as a broken agent.
LIBRECHAT_CODE_API_KEY=
